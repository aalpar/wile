;; vector-sort.scm -- vector sort operations
;; Part of SRFI 132: Sort Libraries
;; Stub: will be implemented in a later task.

(define (vector-sort less? v . rest)
  (error "vector-sort: not yet implemented"))

(define (vector-stable-sort less? v . rest)
  (error "vector-stable-sort: not yet implemented"))

(define (vector-sort! less? v . rest)
  (error "vector-sort!: not yet implemented"))

(define (vector-stable-sort! less? v . rest)
  (error "vector-stable-sort!: not yet implemented"))
