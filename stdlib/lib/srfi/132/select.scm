;; select.scm -- selection and separation for vectors
;; Part of SRFI 132: Sort Libraries
;; Stub: will be implemented in a later task.

(define (vector-select! less? v k . rest)
  (error "vector-select!: not yet implemented"))

(define (vector-separate! less? v k . rest)
  (error "vector-separate!: not yet implemented"))
