;; vector-merge.scm -- vector merge operations
;; Part of SRFI 132: Sort Libraries
;; Stub: will be implemented in a later task.

(define (vector-merge less? v1 v2 . rest)
  (error "vector-merge: not yet implemented"))

(define (vector-merge! less? to v1 v2 . rest)
  (error "vector-merge!: not yet implemented"))
