;; dedup.scm -- neighbor deduplication for lists and vectors
;; Part of SRFI 132: Sort Libraries
;; Stub: will be implemented in a later task.

(define (list-delete-neighbor-dups eq lis)
  (error "list-delete-neighbor-dups: not yet implemented"))

(define (list-delete-neighbor-dups! eq lis)
  (error "list-delete-neighbor-dups!: not yet implemented"))

(define (vector-delete-neighbor-dups eq v . rest)
  (error "vector-delete-neighbor-dups: not yet implemented"))

(define (vector-delete-neighbor-dups! eq v . rest)
  (error "vector-delete-neighbor-dups!: not yet implemented"))
