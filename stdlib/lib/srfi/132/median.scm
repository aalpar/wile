;; median.scm -- median finding for vectors
;; Part of SRFI 132: Sort Libraries
;; Stub: will be implemented in a later task.

(define (vector-find-median less? v knil . rest)
  (error "vector-find-median: not yet implemented"))

(define (vector-find-median! less? v knil . rest)
  (error "vector-find-median!: not yet implemented"))
