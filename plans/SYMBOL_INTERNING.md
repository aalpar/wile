# Symbol Interning

**Status**: Subsumed by `TOP_LEVEL_ENVIRONMENT.md`

The symbol interning design (two gates: Parser and `string->symbol`, both calling `TopLevelEnvironment.InternSymbol()`) is fully described in `TOP_LEVEL_ENVIRONMENT.md`. See that document for the complete design.
