;;; algebra-dataflow-test.scm — (wile algebra dataflow)

(import (scheme base)
        (chibi test))

;; Library scaffold — body populated in Phase 6.
(test-begin "dataflow — scaffold")
(import (wile algebra dataflow))
(test-end)
